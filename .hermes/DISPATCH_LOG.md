# Dispatch Log (APPEND-ONLY — never edit or delete existing lines)

One row per dispatch attempt. `spec` and `result` link JSON
files under `.hermes/run-logs/` (gitignored) or inline gist of key fields.

| task_id | attempt | model | status | verdict | notes |
|---------|---------|-------|--------|---------|-------|